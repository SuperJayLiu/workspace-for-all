# 学术工作台 · 交付说明（v3.11.0）

这份文件回答四个问题：装在哪、怎么用、出事了怎么办、哪些还没做。
读完这一份就够了，其余文档都是细节补充。

---

## 一、它是什么

一个只跑在你自己电脑上的本地服务，加一个浏览器界面。数据是一堆 Markdown 文件，
放在这个文件夹里，你随时可以用任何编辑器打开、用任何工具搜索、用任何方式备份。
没有云端账号，没有订阅，没有第三方能读到你的东西。

两台电脑之间靠一个私有 GitHub 仓库同步；手机靠局域网直连或一份只读简报。
Claude 的自动任务通过同一个仓库读写工作台，所以你合上笔记本它照样能干活。

**一句话架构**：Markdown 是真相，Git 是通道，浏览器是外壳，Python 服务是中间那层胶水。
这四样任何一样坏了，你的数据都还在。

---

## 二、装在哪、怎么起

### Mac

双击 `安装-Mac.command`。它会检查 Python、装好开机自启、起一次服务并打开浏览器。
装完以后你什么都不用做——开机就在后台跑，浏览器打开 `http://127.0.0.1:8765/` 即可。

想取消自启，双击 `卸载自启-Mac.command`。想临时手动起一次，双击 `启动.command`。

### Windows

双击 `安装-Windows.bat`，同样的流程，会在桌面放一个快捷方式。
取消自启双击 `卸载自启-Windows.bat`。

### 想让手机也能连

双击 `局域网启动.command`（Mac）或 `局域网启动.bat`（Windows）。
**前提是先在「设置 → 远程访问与手机」里填一个访问码**——没有访问码它会直接拒绝启动，
因为开了端口又不设码，等于把工作台放在网上裸奔。

---

## 三、日常怎么用

### 手机上怎么进来

在设置页点「生成手机入口页」，它会往你的 OneDrive 目录里放一个 `工作台入口.html`。
手机上收藏这个文件。打开时它会自动去探测 Mac 和 Windows 哪一台开着，只有一台在线就直接跳过去，
两台都在线让你挑。

两台都关机时，还有一份 `今日简报.html` 兜底——纯静态页面，每天早上自动更新，
上面有在进行的稿件、近的会议截稿、接下来的日程和在读文献。手机在 OneDrive 里直接看就行。

### 远程进来能干什么

进来必须输访问码，**默认只读**：可以看，不能改。要改东西，点右上角那把 🔒 锁，
再输一次访问码，解锁 30 分钟，到点自动回到只读。

有几件事远程永远做不了，哪怕解了锁：看密钥、看 Git 凭据、浏览文件系统、执行脚本、
恢复备份、上传表格。这些只能在电脑跟前做。所有失败的访问码尝试都记在 `local/security.log` 里，
设置页有个「查看访问记录」可以直接看。同一个 IP 15 分钟内错 5 次会被锁 15 分钟。

### 两台电脑怎么互相知道

每台机器每 10 分钟往 `data/presence/` 里写一次心跳，通过 Git 同步过去。
设置页的「我的设备」卡会显示每台机器的状态：20 分钟内有心跳算在线，一小时内算最近在线，
再久就是离线。手机入口页用的也是这份数据。

---

## 四、出事了怎么办

**页面打不开。** 先确认服务在跑：浏览器访问 `http://127.0.0.1:8765/api/ping`，
有回应就说明服务活着，问题在前端；没回应就双击一次启动脚本。

**记录好像丢了。** 先别慌，删除的记录不是真删，都在 `local/trash/` 里躺着，文件名后面带时间戳。
直接把 `.md` 文件拷回 `data/对应的库/` 就回来了。

**配置被改坏了。** `local/backups/rolling/` 里有每半小时一份的滚动快照，保留七天；
OneDrive 目录里有每天两份的长期备份。设置页的「备份时光机」可以直接挑一份恢复，
恢复前它会自动把当前状态另存一份，所以恢复错了也能再恢复回来。

**两台机器改了同一条记录。** 后保存的那台会看到一个对比界面，让你选保留哪份，
或者两份都留（对方那份会附在正文末尾）。它不会静默覆盖任何一方。

**同步一直出错。** 顶栏左边那个同步状态点会变红，点它能看到具体错误。
最常见的是 GitHub token 过期——去设置页重填一次即可。

**想确认整个系统还健康。** 在终端里进到这个目录，跑一遍体检：

```
python3 tests/1-后端.py
python3 tests/3-接口与PDF.py
python3 tests/4-鲁棒性.py
python3 tests/5-远程访问.py
python3 tests/8-Overleaf进展.py
python3 tests/10-新功能.py
python3 tests/11-关联后端.py
```

浏览器那五份需要 Node：先 `python3 server.py --port 8799 --no-open &`，再依次跑
`node tests/2-前端.js`、`node tests/6-手机与远程界面.js`、
`node tests/7-AI与快捷链接.js`、`node tests/9-界面改动.js`、`node tests/12-关联界面.js`。
十八份脚本覆盖核心逻辑与界面检查，全绿就说明系统是好的。**跑测试时别在浏览器里编辑东西**——
第 4 份会临时把配置改坏再改回来，还会强杀几次服务进程。

---

## 五、你的东西存在哪、什么会同步

| 内容 | 位置 | 进 Git 吗 | 进备份吗 |
|---|---|---|---|
| 稿件、论文、会议、文献、想法、日程 | `data/` | 是 | 是 |
| 饮食、运动、开支、纪念日、清单、杂务 | `local/life/` | **否** | 是 |
| 密钥（GitHub token、钉钉密钥、访问码、备份口令） | `local/secrets.json` | **否** | **否** |
| 本机路径设置（论文目录、OneDrive 目录） | `local/device.json` | **否** | **否** |
| 设备心跳 | `data/presence/` | 是 | 是 |
| 写作进展（Overleaf 同步出来的） | `data/progress/` | 是 | 是 |
| Overleaf 项目本体（clone 下来的） | `local/overleaf/` | **否** | **否** |
| AI API 用量流水 | `local/ai-usage.jsonl` | **否** | **否** |
| 安全日志 | `local/security.log` | **否** | **否** |
| 回收站 | `local/trash/` | **否** | 否 |

生活数据只留本机，是你一开始就定下的规矩，一直没变。密钥既不同步也不备份——
这意味着换新电脑要重新填一次，但也意味着 GitHub 仓库万一泄露，里面找不到任何能用的凭据。

OneDrive 的每日备份可以加密（设置里打开）。**加密用的口令只存在这台电脑上，丢了就真的打不开**，
这是设计如此，不是可以补救的疏忽。

---

## 六、v3.3 – v3.8 又加了什么

**研究页**：原来的「学术生产」和「稿件库」合成一页——总览统计、进行中的项目卡、
阶段看板、全部稿件、期刊档案、PDF 库，从上到下一条线。稿件库和期刊库的独立页面仍在，
从搜索或 `#manuscripts` / `#journals` 都能直达，只是不再各占一个侧边栏位置。

**Overleaf 写作进展**（需付费版）：每天早上把你的项目 pull 一次，算出改了哪几节、
净增删多少行、正文字数变化，写成当天的进展记录，进今日页、周报和手帐。

**AI 直连 API**（可选）：Claude / ChatGPT / DeepSeek 三选一或都填，支持中转地址。
按量付费，跟订阅是两个钱包；设置页有近 30 天的调用次数与 token 统计。不填不影响任何功能。

**邮箱收件**：手机发邮件 → 工作台每 30 分钟收一次 → 自动进「待分类」。
这是钉钉读不了群消息的替代方案。

**领域助读**：设一个想入门的领域，每周排一个任务给云端 Claude，产出四到六篇经典论文的
中文循序渐进重述 + 五天安排，自动进阅读队列。走订阅，不花 API 钱。

**本周一览**：七天铺开，Outlook 与工作台的所有事项合并显示。

**界面自由度**：布局编辑模式里每张卡有 ⚙，可改标题、限制条数、设默认收起、加备注。
每张含示例的卡上有「🙈 隐藏示例」，一点全站生效。列表支持多选批量删除/存档。
工作台名称可自定义（左上角的 J 是固定标志）。

**表格导入重写**：修了一个会毁数据的解析 bug（空单元格导致整行左移），
加了列名自动映射，没有标题的行直接跳过而不是造「未命名」。

---

## 六之二、v3.9 加的：把所有东西连起来

以前每个库都是孤岛：想法是想法，稿件是稿件，读过的文献和它启发了哪篇稿子之间没有任何线索。
现在**任何一条记录都能关联到任何另一条**——想法 ↔ 稿件、文献 ↔ 稿件、会议 ↔ 稿件、日程 ↔ 任何东西。

打开任一条记录，正文下面有「🔗 关联」：点「＋ 关联到别的记录」，一个搜索框跨所有库找，
选中就连上。关联是**双向**的——你在想法里连了稿件，打开那篇稿件也会看到这条想法。
列表和卡片上会出现一个 `🔗 2` 的小标，鼠标停上去能看到连着哪几条。

三件值得说的设计取舍：

关联存在两条记录各自的 frontmatter 里（`links: [manuscripts:xxx]`），**没有独立的关系文件、没有数据库**。
这意味着你用任何 Markdown 编辑器打开都看得见，导出、备份、三年后换工具都不会丢。
代价是建一条关联要写两个文件，慢一点点——但换来的是这份数据永远不会"只有这个软件能读"。

删掉一条记录时，对面那条里指向它的箭头会自动摘掉，不留死链。万一因为同步冲突还是留了，
chip 会显示成带删除线的「（记录已删除）」，点不动但也不会让页面崩。
设置页之外还有个 `POST /api/link/gc` 可以全库扫一遍。

**新建的记录暂时不能关联**——因为它还没有 id。保存一次再打开就有了，界面里也是这么写的。

---

## 七、已完成与未完成

十二个阶段全部做完：数据层与服务、界面骨架、学术生产流、生活模块、AI 额度调度、
推送与日历、箴言与手帐、GitHub 雷达、首次使用向导、双机互补与手机、安全加固、收尾。
之后又做了 v3.3–v3.8 的六轮增补（见上一节）。

**测试现在是十八套**：13 个 Python 核心与安全测试，加 5 个 Playwright 界面测试。
可直接运行 `bash tests/跑全部.sh`；未安装 Playwright 时会自动跳过界面部分。

三轮极端测试共发现并修复 40 余个问题，其中比较要紧的几个：记录 id 可以写到数据目录之外（已堵）、
Windows 非法文件名会让整个仓库同步失败（已挡）、写文件不是原子的（已改）、
含中文的访问码会让登录崩溃且锁定失效（已修）。

**GitHub 同步现在会说实话**：以前推送失败只在内存里留个错误，服务一重启就查不到了；
现在成败都写进 `local/sync.log`，设置页的 Git 卡上会直接标出「本地有 N 个提交还没推上去」，
从来没推成功过还会单独警告。日志里的 token 一律打码。

**还留着的两个已知边界**，都是权衡后按设计保留的：

订阅日历里带 TZID 的日程按字面时间显示，不做完整时区换算。如果你的 Outlook 发布的是伦敦时间
而你人在杭州，看到的是伦敦的钟点。要修得内置一整套 IANA 时区表，体积不小；建议先用着，
真发现偏差再补。

速记的分类关键词用了正则的否定环视，Safari 16.4 以前的版本不支持。Chrome、Edge、
新版 Safari 都没问题。

**没做的**：没有多人协作（这是一个人的工作台）；没有移动端原生 App（手机走浏览器）；
没有把 PDF 全文索引起来（只提取标题和作者）。这三件都不难做，只是对单人日常使用来说优先级不高。

**还没在真实环境里验证过的**（代码写了、单元测试过了，但没连过真东西）：
Overleaf 只用本地 git 仓库模拟测过；邮箱收件没有连过真实邮箱；
Windows 那台机器没有实际安装过，所以「双机互补」没有真的双机跑过；
手机远程访问没有在真手机上开过。这些要真正用起来才知道，遇到问题欢迎提 issue。

**依赖 Git 仓库才能生效的功能**：额度调度器、自动任务队列、周一早报的自动推送、
GitHub 雷达、全库体检、领域助读——这些都要通过私有仓库交给云端的 Claude 执行。
**仓库没初始化之前，它们的代码都在、逻辑都对，但不会真的运行。**
这是第一件该做完的事（设置 → Git 同步 → 初始化仓库）。

---

## 八、如果哪天你想自己改

服务端一个文件 `server.py`，纯标准库，没有任何 pip 依赖——这是刻意的，
因为依赖越多，三年后越可能装不起来。前端是 `app/` 下面十几个 js 文件，
每个对应一个模块，没有构建步骤，改完刷新页面就生效。

改之前跑一遍测试，改之后再跑一遍。两次都全绿，就可以放心。
