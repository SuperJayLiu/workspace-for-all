# 学术工作台 · 使用教程

写给第一次上手的人。从"它是什么"到"每天怎么用"，中间那些要填的东西一个一个说清楚。
配置部分照着做就行，不需要懂技术。

这份工作台不绑定任何特定的人或学校：下面提到的 GitHub 仓库、OneDrive 目录、钉钉群、
Overleaf 项目，都是你自己的账号。整套东西跑在你自己的电脑上，没有服务器，没有后台，
也没有人能看到你的数据。

---

## 一、它是什么，能帮你做什么

一个跑在你自己电脑上的学术管理台。打开浏览器就能用，但数据不在任何云端服务器上——
它就是你硬盘上的一堆 Markdown 文件，用任何编辑器都能打开，哪天不用这个界面了，记录也还在。

它围绕一篇论文的真实生命周期转：**选题 → 写作 → 投稿 → 审稿 → 转投 → 发表 → 归档 → 复盘**。

**学术这一侧**

- **稿件库**：每篇在写的论文一张卡，记进度、下一步该干什么、合作者、目标期刊。点「记录事件」把投稿、拒稿、R&R、转投一条条记下来，阶段自动跟着变。坚持记这一件事，你就能自动得到"我在这个期刊真实等了多久"的统计。
- **论文库**：已发表的成果，引用、期刊、年份。
- **学术会议**：截稿日期倒计时，快到了会出现在今日页顶部。
- **读文献**：每篇记「问题背景 / 方法 / 结论」三段，读完加经验值，像闯关一样。按 1/7/30/90 天自动排复习队列。
- **想法**：灵感随手扔进去，不用当场想清楚。
- **研究页**：一页看清全部——总览统计、进行中的项目卡、阶段看板（可拖）、全部稿件、期刊档案、PDF 库。PDF 会自动提取标题，不用你手动录。
- **领域助读**：设一个你想入门的领域，工作台每周排一个任务给 Claude，挑出该领域最经典的四到六篇，按由浅入深排好，每篇写一段中文重述，再给一份五天的安排。

**生活这一侧**（这部分数据只留本机，永远不上传）

饮食、运动、开支、纪念日、待办清单、杂务。

**每天陪着你的**

- **今日页**：逾期的、快到期的、稿件的下一步、该复习的文献、AI 昨晚干了什么，一屏看完。顶部四个统计卡都能点开看明细。
- **本周一览**：七天铺开，Outlook 日程、工作台日程、会议截稿、稿件下一步、生活杂务全合在一起。
- **今天完成了什么**：速记框里记的完成记录会汇到今日页，显示今天几件、本周几件、记了多少分钟。
- **右上角日历**：公历 + 农历 + 节气 + 天气 + 你订阅的 Outlook 日程。
- **右下角速记框**：像聊天一样随手记，它自己判断这条是想法、是任务、还是待分类。
- **每日箴言**：四百多条中英文，每天换一条。
- **周一早报**：周一早上 8 点自动生成上周复盘 + 本周开局，推到你手机。

**替你干活的**

工作台会盯着你订阅额度的使用节奏，在你不用的时段（夜里、周末）自动派活给 Claude：
头脑风暴、找漏洞、扫文献缺口、全库体检、GitHub 雷达。订阅额度用不完也不会滚存到下周，
所以这件事的本质是"把本来要蒸发的额度换成产出"。

---

## 二、需要准备什么

**必须的**

- 一台 Mac 或 Windows 电脑，装了 Python 3.8 以上（Mac 一般自带；没有的话安装脚本会告诉你怎么装）
- 一个浏览器（推荐 Chrome）
- 一个 **私有** GitHub 仓库（免费账号就行）——两台电脑之间靠它同步

**可选，但强烈建议**

- **OneDrive**（或其它网盘的本地同步文件夹）：放长期备份和手机看的简报
- **钉钉**：接收周一早报和提醒（用钉钉是因为它免费、支持自定义机器人、手机弹窗可靠）

**完全可选**

- **Overleaf 付费版**：有的话工作台能自动读出你每天在论文上改了什么，替你记进展（见 4.4）
- Claude、OpenAI 或 DeepSeek 的 API key：填了可以在工作台里直接问 AI。**不填也不影响任何功能**，
  自动任务走的是订阅那条路，跟 API 是两码事。

---

## 三、装起来（三分钟）

**Mac**：双击 `安装-Mac.command`。它会检查环境、配好开机自启、启动服务并打开浏览器。

**Windows**：双击 `安装-Windows.bat`，同样的流程，会在桌面放个快捷方式。

装完以后开机就自动在后台跑，浏览器打开 `http://127.0.0.1:8765/` 就是你的工作台。
不想要开机自启，双击对应的 `卸载自启-*` 文件即可。

第一次打开会弹出配置向导，一共十来步。**每一步都能跳过**，跳过的以后在「设置」页随时能补。
向导第一步可以给工作台起个名字（左上角那个 J 是这套工作台的标志，固定不变）。
下面第四节讲的就是向导里那几步要填的东西怎么拿到。

**用熟之后记得把示例数据藏掉**：种子里带了十几条「（示例）」开头的记录，是给你看结构用的。
任意一张卡片右上角有「🙈 隐藏示例」，点一次全站生效；
「设置 → 🌱 示例数据」里可以再显示回来，或者彻底删掉。

---

## 四、三样外部服务，一个一个配

### 4.1 GitHub：两台电脑之间的桥

**它负责什么**：Mac 和 Windows 之间的数据同步，以及云端 Claude 读写你的工作台。
没有它，工作台在单机上也能用，只是这两件事没有。

**第一步，建一个私有仓库。** 登录 GitHub → 右上角 `+` → New repository →
名字随便起（比如 `workspace`）→ **一定要选 Private** →
下面的 "Add a README file" 之类**全都不要勾**（要一个空仓库）→ Create。

> 为什么必须私有：里面会有你的稿件、审稿意见、投稿记录和还没成形的想法。

**第二步，生成一个 token。** GitHub 用 token 代替密码来验证身份。

登录 GitHub → 右上角头像 → **Settings** → 左侧菜单拉到最底 **Developer settings** →
**Personal access tokens** → **Tokens (classic)** → 右上 Generate new token → Generate new token (classic)。

填三处：Note 随便写（比如 `workspace`）；Expiration 选 90 天或 1 年；
**Scopes 里只勾最上面那个 `repo` 大框**（勾它会自动带上下面几个子项，这是对的），其它一个都别动。
拉到底点 Generate token。

生成的字符串以 `ghp_` 开头，**那一屏关掉就再也看不到了**，先复制下来。

> 也有一种"细粒度 token"（Fine-grained），能精确到只授权某一个仓库，更安全，但设置项多、容易漏，
> 漏了会报 403 而且提示很含糊。第一次配建议先用经典 token 把链路跑通，之后想收紧再换。

**第三步，填进工作台。** 向导的「GitHub 同步」那一步，或者「设置 → Git 同步」：
仓库地址填 `https://github.com/你的用户名/仓库名.git`，用户名填你的 GitHub 用户名，
Token 填刚才那串。点「初始化仓库」，它会在你电脑上完成初始化、首次提交、推送这一整套。
顶栏左边的同步点变绿就是成功了。

**第二台电脑**：装好工作台后，同样填这三项，点初始化，它会把仓库拉下来。之后两台就自动同步了。

**token 过期了怎么办**：同步会开始报错，顶栏的点变红。重新生成一个填进去即可，其它都不用动。

---

### 4.2 OneDrive：长期备份和手机能看的简报

**它负责什么**：每天两次的长期备份、手机入口页、每日只读简报。
**它不负责两台电脑之间的数据同步**——那是 GitHub 的活（下一节专门解释）。

**关键点：工作台要的是"本地路径"，不是网页链接。** 工作台是往你硬盘上写文件，
写完之后由 OneDrive 客户端自己上传。所以那种 `https://xxx.sharepoint.com/...` 的分享链接在这里没用。

**第一步，确认 OneDrive 客户端装了并且在同步。** Mac 顶部菜单栏、Windows 右下角托盘，
应该有个云朵图标。没有就去 [onedrive.com](https://www.onedrive.com) 或应用商店装一个，用你的账号登录。

**第二步，在 OneDrive 里建一个文件夹**，比如叫 `workspace` 或 `工作台备份`。

**第三步，找到它在你电脑上的真实路径。**

Mac：打开终端（启动台 → 其他 → 终端），粘贴这一行回车：

```
ls -d ~/Library/CloudStorage/*/
```

会列出类似 `/Users/你的用户名/Library/CloudStorage/OneDrive-学校名/` 的路径，
后面拼上你刚建的文件夹名就是完整路径。或者更简单：在访达里找到那个文件夹，
右键 → 按住 Option 键 → 点「拷贝"xxx"为路径名称」。

Windows：在文件资源管理器里打开那个文件夹，点一下地址栏，它会变成
`C:\Users\你\OneDrive - 学校名\workspace` 这样的文本，直接复制。

**第四步，填进工作台**：向导的「本机设置」那一步，或者「设置 → 本机设置 → OneDrive 长期备份目录」。
填完保存，然后去「设置 → 备份时光机」点一次「立即备份」，
再去那个文件夹里看看有没有出现 zip 文件——有就说明通了。

**备份要不要加密**：设置里可以打开。加密后即使网盘账号被盗，备份也打不开。
但**口令丢了就真的打不开**，没有找回机制。

---

### 4.3 钉钉：把提醒推到手机

**它负责什么**：周一早报、每日简报、需要你拍板的提醒。

**为什么是钉钉不是微信**：钉钉官方支持自定义机器人，免费、五分钟配好、手机弹窗可靠。
个人微信的第三方机器人方案有封号风险，不值得。

**第一步，跟自己建一个群。** 打开钉钉 → 右上角 `+` → 发起群聊 → 随便选一个人建群
（建完可以把对方移出去，只剩你自己）→ 群名改成比如「我的工作台」。

> 钉钉不允许建只有一个人的群，所以要先拉一个人再移出去。移出后群还在，机器人照常工作。

**第二步，加一个自定义机器人。** 在这个群里 → 右上角群设置（三条杠或齿轮）→
**智能群助手** → **添加机器人** → 在列表**最下面**选「**自定义**·通过 Webhook 接入自定义服务」。

> 注意别选成机器人市场里的成品机器人（比如 GitHub 机器人）。那种只认自己的消息格式，
> 发别的内容会报 `300001 robot type do not match`。

**第三步，安全设置。** 添加时会让你选三选一，**只勾「加签」一个**，
另外两个（自定义关键词、IP 地址）**不要勾**——勾了又不填，要么存不下，要么消息全被拒。

**第四步，拿到两样东西。**

1. 勾了加签之后，那一栏会出现一串 `SEC` 开头的密钥，点旁边的 **Copy** 复制（输入框会截断显示，一定要用 Copy）
2. 完成后会给你一个 **Webhook 地址**，形如 `https://oapi.dingtalk.com/robot/send?access_token=...`

**第五步，填进工作台**：向导的「推送渠道」那一步，或者「设置 → 推送渠道」。
Webhook 和 SEC 密钥各填一栏，保存，然后点「发送测试消息」，去群里看收到没有。

**常见报错**：`310000` 是安全校验没过——如果消息里写着 `sign not match` 就是 SEC 密钥抄错了，
写着 `keywords not in content` 说明你勾的其实是关键词模式。工作台会把这些错误码翻译成人话显示。

---

### 4.4 Overleaf：让它替你记「今天写了什么」（可选，需要付费版）

**它负责什么**：每天自动读一次你的 Overleaf 项目，算出改了哪几节、净增删多少行、
正文字数变化多少，写成一条进展记录。今日页、周报、手帐都会自动用上——
你不用再手动回忆"这周论文推进了多少"。

**为什么要付费版**：Git 接入是 Overleaf 的付费功能。免费版用户可以跳过这一节，
仍然能在每篇稿件上填 Overleaf 链接做一键跳转，只是没有自动进展记录。

**第一步，生成 Git token。** Overleaf 网页 → 右上角头像 → **Account Settings** →
左侧 **Git Integration** → **Generate token**。会得到一串 `olp_` 开头的字符。
注意这**不是**你的登录密码，是专门给 Git 用的。

**第二步，填进工作台。** 向导里有「Overleaf 写作进展」这一步，或者
「设置 → 📝 Overleaf 写作进展」，填登录邮箱和 token，保存。

**第三步，把项目挂到稿件上。** 稿件库里任意一张卡点「编辑」，
「Overleaf 项目地址」那一栏粘上项目链接（形如
`https://www.overleaf.com/project/xxxxxxxxxxxx`，直接给项目 id 也行）。

填完回设置页点「立即同步全部项目」。第一次要 clone，可能等十几秒，而且只记一个基线；
从第二次开始才有"这段时间改了什么"。之后每天早上八点后自动同步一次。

**关于隐私**：项目只 clone 到你电脑的 `local/overleaf/`，不进你的 GitHub 仓库，
也不进备份。token 只存在本机，任何日志里都不会出现明文。

---

### 4.5 邮箱收件：在手机上随手往工作台丢东西（可选）

**它负责什么**：你从手机发一封邮件，工作台每 30 分钟收一次，自动变成「待分类」的条目。

**为什么要这个**：钉钉的自定义机器人是单向的——能发不能收，读不了群消息。
所以想在手机上随手记点什么、又不想开浏览器，走邮箱最稳。

**怎么配**：「设置 → 📮 邮箱收件」，填 IMAP 服务器、端口、账号、密码，打开开关，点「立即收一次」验证。

- **密码多半要用「授权码」而不是登录密码**。QQ 邮箱、163、Outlook 都要先在邮箱设置里开启 IMAP 并生成一个授权码。
- 建议填上「只收来自」（你自己的邮箱地址），这样别人发进来的邮件不会被吸进工作台。
- 也可以要求标题必须含某个标记（比如 `#台`），这样同一个邮箱还能正常收别的信。

常见服务器地址：QQ 邮箱 `imap.qq.com`、163 `imap.163.com`、
Outlook / 学校邮箱 `outlook.office365.com`，端口都是 993。

---

### 4.6 学术雷达：每周替你盯着有没有新论文（可选）

**它负责什么**：你填几个关键词、几个要盯的人，它每周一早上去几个源捞一遍新东西——
新发表的、新挂出来的 working paper——挑出值得看的，写进那周的周报，并存进论文库。

**怎么配**：「设置 → 🛰 学术雷达」。

- **关键词**：不填就沿用「研究方向」里的那几个。建议 3–8 个，太宽了每周会被淹。
- **关键人**：这里有个坑要说清楚。**能填 ORCID 就一定填**——
  Crossref 按人名搜是**模糊匹配**，实测搜 "Zhiguo He" 会把所有姓 He 的人都返回来，
  根本没法用来盯人。没填 ORCID 的会退化成「姓名搜索 + 姓氏复核」，
  并且**在周报里标明「可能不是同一个人」**，不会假装追到了。
  ORCID 在 orcid.org 搜人名就能查到，形如 `0000-0002-1825-0097`。
- **源**：Crossref（覆盖绝大多数期刊，也包含 NBER working paper）、
  NBER（每周新挂的）、arXiv。默认三个都开。
- **mailto**：填个邮箱，Crossref 会把你的请求放进更快的队列。可以不填。

**先自检一遍**。这一步别跳过——学校和公司的网络代理经常会挡掉外网 API，
而挡掉之后的表现是「这周什么都没抓到」，跟「这周确实没新东西」长得一模一样。
点那个「测一下各个源通不通」，或者命令行：

    python3 scripts/radar.py --selftest

三个源各报一行通不通。全挂多半是网络代理的事，换个网络再试。

**它是怎么保证不给你编论文的**——这一段值得看一眼，因为这是这个功能最容易出事的地方。

让 AI 直接上网找论文，它会编。而且编出来的东西标题像真的、作者像真的、DOI 也像真的，
你不去查根本看不出来。所以这条流水线是拆开的：**抓取全部由死代码完成，AI 完全不参与**，
抓到什么原样存进候选池；**AI 只在这个池子里挑**，并写成人话；写完之后
**再由一段代码逐条核对**——指纹在不在池子里、标题对不对得上、年份差没差、
提到的作者是不是真在作者名单里。核对是纯字符串比对，不是再叫一个模型来判断
（用模型验模型，等于让同一类错误互相背书）。没通过的直接丢，
并且**在周报里如实写明这次丢了几条**。

所以你在周报里看到的每一篇，都是真从源里抓下来的。要是哪周抓取本身失败了，
周报会直接写「这周没抓到」，不会拿旧数据凑一份看起来一切正常的报告。

---

## 五、同步到底是怎么回事（回答一个常见疑问）

这里有两套完全独立的机制，很多人会搞混：

| | 谁负责 | 同步什么 | 多久一次 |
|---|---|---|---|
| **两台电脑之间的数据** | **GitHub** | 稿件、论文、会议、文献、想法、日程 | 每 5 分钟自动拉一次，你点「同步」立刻推 |
| **长期备份和手机简报** | **OneDrive** | 备份 zip、入口页、每日简报 | 工作台每天写两次，OneDrive 客户端自己上传 |
| **论文写作进展** | **Overleaf 的 Git** | 只读你的论文项目，算出改了什么 | 每天早上一次，也可以手动点 |

**所以：跨设备同步不依赖 OneDrive，也不需要 OneDrive 实时同步。**
就算你的 OneDrive 停了几个小时，Mac 和 Windows 之间的数据照样同步——那条路走的是 GitHub。

OneDrive 那边也不需要你做任何"手动同步"的动作。工作台只管把文件写到那个本地文件夹里，
OneDrive 客户端看到文件变了就自己传上去，这是它本来就在干的事。你要确认的只有一件：
**那个客户端是开着的**（菜单栏/托盘里有云朵图标，且不是暂停状态）。

**一个要避开的坑**：不要把工作台文件夹本身放进 OneDrive 同步范围里。
OneDrive 和 Git 都会去动同一批文件，两边打架会把仓库搞坏。
工作台放在普通目录（比如 `文稿/工作台`），OneDrive 只用来接收备份文件，这样最稳。

---

## 六、每天怎么用

**早上**打开「今日」页：逾期的、这几天要交的、每篇稿件的下一步、该复习的文献、
AI 昨晚产出的东西，都在这一页。

**随时**想到什么就按 `c`（或点顶栏「⚡ 捕捉」），像发消息一样打一行字。
它会自己判断这是想法、任务还是日程，判不准就丢进「待分类」，不猜、不乱填。

**投稿有动静时**，去稿件卡点「＋ 记录事件」记一条。**这是整个系统最值得坚持的习惯**——
记满几次之后，你的真实审稿周期、拒稿率、平均耗时就都自动有了，复盘时不用翻邮件。

**读完一篇文献**，在「读文献」里记「问题背景 / 方法 / 结论」三段，加经验值。
过几天它会自动把这篇排进复习队列。

**有大活要给 AI**，点顶栏「🤖 信箱」写进去，云端的 Claude 会在你不用电脑的时候处理。

**记完一条之后**，屏幕下方会给一句回应，旁边有个「🤖 拿去问 AI」——
点了会带着一段写好的话跳进 AI 面板。比如记完一餐，那段话是让 AI 估热量、
指出最值得改的地方、给两个更均衡的替代；记完一个想法，是让 AI 把它逼成一个可检验的经济学问题。
八类记录各有各的话术，都是预先写好的文本，不花任何 AI 成本。

**想把两件事连起来**：打开任意一条记录（想法、稿件、文献、会议、日程都行），
正文下面有「🔗 关联」，点「＋ 关联到别的记录」，搜一下标题就能连上。
关联是双向的——你在想法里连了某篇稿件，打开那篇稿件也会看到这条想法。
列表和卡片上会出现 `🔗 2` 这样的小标，鼠标停上去看得到连着哪几条，点一下直接跳过去。

这是这个工作台里最容易被忽略、但半年后最值钱的一个动作：
等你手上有五六篇稿件、几十篇文献的时候，"这篇稿子的想法当初是从哪来的"、
"这个方法我在哪篇文献里见过"，靠记忆是记不住的，靠关联一点就到。
建议至少养成两个习惯：**想法升级成稿件时连一条**，**文献启发了某篇稿件时连一条**。

（刚新建、还没保存的记录不能关联——它还没有 id。存一次再打开就行。）

**想批量整理**：读文献、论文库、学术会议的列表里，每行左边有勾选框。
选中若干条，屏幕底部会浮出操作条，可以一次删除或存档。
删除都是移到 `local/trash/`，捞得回来。

**觉得哪里不顺手**：点顶栏「🎛 布局」进编辑模式。除了拖 ⠿ 排序、点 👁 隐藏，
每张卡上还有一个 **⚙**：可以改这张卡的标题、限制最多显示几条、设默认展开还是收起、
给它加一句自己的备注。改完即时生效，也随时能一键恢复默认。

**快捷键**：`c` 捕捉 · `n` 打开速记框 · `d` 看已完成 · `Cmd/Ctrl+K` 全局搜索 · `Esc` 关窗。

---

## 七、手机上怎么用

有两种，一起用效果最好。

**一是直连**（能改东西）。在工作台「设置 → 远程访问与手机」里设一个访问码，
然后用 `局域网启动` 那个脚本启动（不是平时那个）。手机连同一个 Wi-Fi，
在「设置 → 远程访问与手机」里点「生成手机入口页」，把生成的 `工作台入口.html` 放进 OneDrive，
手机上收藏它。打开时它会自动找哪台电脑开着并跳过去。

进去要输访问码，而且**默认只读**——能看不能改。要改就点右上角那把锁再输一次访问码，
解锁 30 分钟。密钥、文件浏览这些事，远程永远碰不到。

**二是每日简报**（只能看，但不挑条件）。工作台每天早上往 OneDrive 写一份
`今日简报.html`，纯静态页面。两台电脑都关机、你人在外面，照样能在手机的 OneDrive 里打开看。

---

## 八、出问题了怎么办

**页面打不开**：先访问 `http://127.0.0.1:8765/api/ping`，有回应说明服务活着，
问题在页面（刷新一下）；没回应就双击一次启动脚本。

**记录不见了**：删除不是真删，都在工作台目录的 `local/trash/` 里，文件名带时间戳，
拷回原来的文件夹就回来了。

**想退回几小时前的状态**：「设置 → 备份时光机」，每半小时一份快照，保留七天。
恢复前它会自动先存一份当前状态，所以恢复错了还能再恢复回来。

**两台电脑改了同一条记录**：后保存的那台会弹出对比界面让你选，
或者两份都留（对方那份附在正文末尾）。它不会静默覆盖任何一方。

**同步一直报错**：顶栏左边的点会变红，点它看具体原因。最常见的是 token 过期，重新生成一个填进去。

**想确认整个系统还健康**：在工作台目录里挨个跑 `tests/` 下的脚本，全绿就是好的。
Python 那几份（1、3、4、5、8、10）直接跑；浏览器那几份（2、6、7、9）要先
`python3 server.py --port 8799 --no-open &` 再跑。详见 `tests/README.md`。

---

## 九、附：AI 直连（想清楚再开）

工作台默认不碰 API：自动任务通过 GitHub 仓库交给云端的 Claude，走的是你的**订阅**，不额外花钱。

如果你另外填了 Claude、OpenAI 或 DeepSeek 的 API key（向导里那一步，或「设置 → AI 直连 API」），
就多一条路：在工作台里直接问、直接让它改，不用来回复制粘贴。
三家都支持填「中转地址」，国内直连不畅时可以走中转；DeepSeek 通常是三家里最便宜、直连最稳的。

**但要分清楚两个钱包**：API 是按调用量付费的，跟你的网页订阅完全无关，
订阅再贵也不会覆盖 API 账单。设置页里有近 30 天的调用次数和 token 统计，随时能看用了多少。

不填这一项，工作台所有功能照常。

---

## 十、你的东西存在哪

工作台文件夹里：

- `data/` —— 学术数据，进 GitHub，两台电脑同步
- `local/life/` —— 饮食、运动、开支等，**只在本机**，不进 GitHub
- `local/secrets.json` —— 所有密钥（GitHub token、钉钉密钥、访问码、备份口令），
  **不进 GitHub，也不进备份**。换电脑要重新填一次，但也意味着仓库万一泄露，里面找不到任何能用的凭据。
- `local/trash/` —— 回收站
- `local/backups/` —— 滚动快照

想直接看某条记录的原始文件，去 `data/对应的库/` 找那个 `.md` 就是。

---

## 十一、它和 Cowork 是什么关系

这个工作台是在 **Claude 的 Cowork** 里做出来的，也可以继续在 Cowork 里改。
两者的分工是这样的：工作台是你每天用的界面和数据；Cowork 是你跟 Claude 说
"给我加个功能 / 这里不好用 / 帮我把这张表导进去"的地方。

**要改工作台**：在 Cowork 里开一个会话，把工作台文件夹连上（或者直接描述你要什么），
说清楚想改什么。改完的文件会直接落到你的电脑上，刷新页面就生效。
不需要你懂代码，但如果你愿意看，源码就在这个文件夹里，服务端只有一个 `server.py`，
纯标准库、没有任何依赖。

**要让 Claude 定期替你干活**：工作台通过那个私有 GitHub 仓库跟云端的 Claude 打通。
你在工作台里点「🤖 信箱」写一句话，云端的 Claude 会在你不用电脑的时候读到并处理，
结果写回仓库，你这边一同步就看到了。这条路走的是你的 Claude 订阅，不额外花钱。

几个可能用得上的链接：

- Claude（网页）：<https://claude.ai>
- Claude 桌面应用（Cowork 在这里）：<https://claude.ai/download>
- Cowork 与 Claude Code 的说明文档：<https://docs.claude.com>
- 想给 Claude 提反馈：任意一条回复下面的 👎 按钮

**关于"给所有人用"**：这套东西没有任何一处写死了某个人的信息。
你把整个文件夹拷给同事，他双击安装脚本、走一遍向导填自己的账号，就是他自己的工作台了。
唯一要提醒的是：`local/` 目录里存着密钥和生活数据，**拷贝给别人之前先把 `local/` 删掉**。
